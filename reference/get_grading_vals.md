# Extract grading values using plot vars list and afmm data

Extract grading values using plot vars list and afmm data

## Usage

``` r
get_grading_vals(input_plots, afmm_data)
```

## Arguments

- input_plots:

  `[list(1+)]`

- afmm_data:

  `[list(1+)]`

## Value

`[character(1+)]`
