# Run an example for Patient profile integrated in the module manager

Launches an example app that shows a patient profile module integrated
in the module manager surface. Displays data from the safetyData
package.

## Usage

``` r
mock_with_mm_app()
```
