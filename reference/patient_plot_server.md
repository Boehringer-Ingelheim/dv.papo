# Create server for patient plot shiny module of dv.papo

TODO: Document params (inherit them)

## Usage

``` r
patient_plot_server(
  id,
  subject_var,
  subject_level_dataset,
  timeline_info,
  extra_datasets,
  range_plots,
  value_plots,
  vline_vars,
  vline_day_numbers,
  palette,
  x_axis_unit,
  x_axis_breaks
)
```
